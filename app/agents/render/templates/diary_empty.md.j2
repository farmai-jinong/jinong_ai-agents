# 영농일지 — {{ d.prdlst_nm }} ({{ d.diary_date }})

| 항목 | 값 |
|---|---|
| 작성일자 | {{ d.diary_date }} |
| 작물 | {{ d.prdlst_nm }}{% if d.prdlst_code %} ({{ d.prdlst_code }}){% endif %} |
| 생육단계 | {{ d.gs_nm or "정보 없음" }} |
| 정식일 | {{ d.growing_season_start or "미등록" }} |
| 통화 | {{ call.call_id }}{% if call_when %} ({{ call_when }}){% endif %} |

## 주요 농작업
- 언급 없음

## 기타 기록사항
{% if d.status == "UNRESOLVED_CROP" %}
통화에서 작물을 확정할 수 없어 일지 초안을 만들지 못했습니다. 작물을 지정한 뒤 다시 생성하세요.
{% else %}
통화에서 이 작물의 영농일지에 기록할 농작업·관찰·병해충·투입 내용이 확인되지 않았습니다.
{% endif %}

## 병해충
- 언급 없음

## 방제이력
- 언급 없음

## 농작업 사진
- 통화에서 첨부 가능한 사진 없음

## 투입 제품 (농약·비료·종자·농자재)
- 언급 없음

## 향후 작업·확인 계획
- 언급 없음
{% if d.warnings %}

## 참고
{% for w in d.warnings %}
- {{ w }}
{% endfor %}
{% endif %}

---
_AI 초안 — 농가 확인 후 저장. 생성: {{ generated_at }}, 모델 {{ model or "-" }}, 프롬프트 v{{ prompt_version }}_
