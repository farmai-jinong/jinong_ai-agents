"""STT 용어 오청 복구 실험 — 전사 완료본 + 도메인 용어 카탈로그 → LLM 이 치환 목록 제안 → 결정적 적용 → 채점.

`:8105` 후처리 엔진(자모 fuzzy 검색)은 오청 128건 중 92건이 편집 예산 밖이라 이득 0 이었다
(`docs/stt-postprocess-effectiveness-2026-09-03.md`). 이 하네스는 검색 대신 LLM 에 카탈로그(품종 제외 ~4.2k)를
통째로 주고 **말이 안 되는 어절만** 카탈로그 용어로 바꾸게 했을 때 정답 용어 recall 이 오르는지, 치환 precision 이
버티는지를 오프라인으로 잰다. 파이프라인 배선은 이 수치를 본 뒤 별도 결정(계획 2단계).

런타임 모듈(카탈로그·적용·LLM 호출)은 `app/agents/term_fix/`, 여기는 채점·CLI 만. 자가 개선 루프(`optimize/`)의 DENYLIST 안이다.
"""
