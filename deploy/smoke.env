# 배포 후 스모크 E2E 에 쓰는 고정 실녹음 좌표(비밀 아님). scripts/verify_deploy.sh 가 읽는다.
# 백엔드 MinIO `voice-recordings` 버킷(읽기 전용). 2026-09-11 통화 — 딸기·파프리카 두 작물 PARTIAL 이 안정적으로 나온다
# (dev call dev-multicrop-summary-202609141446 로 확인). 녹음이 삭제되면 여기만 바꾼다.
SMOKE_AUDIO_BUCKET=voice-recordings
SMOKE_AUDIO_KEY=records/1/calls/2026/09/11/a83127aa-e115-4b59-9cfb-b7d874d6c78a/pyhBJ3FzHqonBs5a5cPhG_20260911_160722.ogg
